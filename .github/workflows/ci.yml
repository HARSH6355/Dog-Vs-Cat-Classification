# ============================================================
# CI Pipeline — Cat vs Dog MLOps
# ============================================================
# Triggers: every push & pull request to main branch
#
# Jobs:
#   1. test          — Install deps, run all 47 unit tests
#   2. build-and-push — Build Docker image & push to GHCR
#                       (only on push to main, after tests pass)
#
# Image published at:
#   ghcr.io/harsh6355/dog-vs-cat-classification:latest
#   ghcr.io/harsh6355/dog-vs-cat-classification:<sha>
# ============================================================

name: CI — Test, Build & Publish

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

# ──────────────────────────────────────────────────────────────────────────────
# Job 1: Automated Testing
# ──────────────────────────────────────────────────────────────────────────────
jobs:
  test:
    name: Run Unit Tests
    runs-on: ubuntu-latest

    steps:
      # 1. Check out the repository code
      - name: Checkout code
        uses: actions/checkout@v4

      # 2. Set up Python 3.10 (matches training environment)
      - name: Set up Python 3.10
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      # 3. Cache pip dependencies to speed up subsequent runs
      - name: Cache pip packages
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      # 4. Install CPU-only PyTorch (no GPU in CI — lightweight & fast)
      - name: Install CPU PyTorch
        run: |
          pip install --upgrade pip
          pip install torch==2.5.1+cpu torchvision==0.20.1+cpu torchaudio==2.5.1+cpu \
            --index-url https://download.pytorch.org/whl/cpu

      # 5. Install remaining project dependencies
      - name: Install project dependencies
        run: |
          # Install without torch lines (already installed above)
          pip install \
            numpy==1.26.4 \
            Pillow==10.3.0 \
            scikit-learn==1.5.0 \
            pandas==2.2.2 \
            mlflow==2.14.1 \
            "setuptools<70" \
            kagglehub==0.3.4 \
            matplotlib==3.9.0 \
            seaborn==0.13.2 \
            PyYAML==6.0.1 \
            pytest==8.2.2 \
            pytest-cov==5.0.0 \
            fastapi==0.111.0 \
            uvicorn==0.30.1 \
            python-multipart==0.0.9 \
            tqdm==4.66.4 \
            requests==2.32.3 \
            "httpx>=0.27.0"

      # 6. Run all unit tests with coverage report
      - name: Run unit tests
        run: |
          pytest tests/ -v --tb=short --cov=src --cov-report=term-missing
        env:
          PYTHONPATH: .

      # 7. Upload test results as artifact (optional — good for debugging)
      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: pytest-results
          path: |
            .coverage
          retention-days: 7

  # ──────────────────────────────────────────────────────────────────────────────
  # Job 2: Build Docker Image & Push to GHCR
  # ──────────────────────────────────────────────────────────────────────────────
  build-and-push:
    name: Build & Push Docker Image
    runs-on: ubuntu-latest
    needs: test                    # Only runs if all tests pass
    # Only push on direct pushes to main (not on PRs — security best practice)
    if: github.event_name == 'push'

    permissions:
      contents: read
      packages: write              # Required to push to GHCR

    steps:
      # 1. Check out the repository
      - name: Checkout code
        uses: actions/checkout@v4

      # 2. Set up Docker Buildx (enables advanced build features & caching)
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      # 3. Log in to GitHub Container Registry
      #    GITHUB_TOKEN is automatically provided — no secrets to configure!
      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # 4. Extract metadata for Docker tags and labels
      #    Automatically creates tags: latest, <branch>, <sha>
      - name: Extract Docker metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=raw,value=latest,enable={{is_default_branch}}
            type=sha,prefix=sha-,format=short
            type=ref,event=branch

      # 5. Build the Docker image and push to GHCR
      #    Uses BuildKit cache to speed up repeated builds
      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      # 6. Print the image digest for traceability
      - name: Image published
        run: |
          echo "✅ Docker image published to GHCR"
          echo "📦 Tags:"
          echo "${{ steps.meta.outputs.tags }}"
          echo ""
          echo "🚀 Pull & run with:"
          echo "  docker pull ghcr.io/${{ github.repository }}:latest"
          echo "  docker run -p 8000:8000 ghcr.io/${{ github.repository }}:latest"

  # ──────────────────────────────────────────────────────────────────────────────
  # Job 3: CD — Deploy & Smoke Test
  # ──────────────────────────────────────────────────────────────────────────────
  deploy:
    name: Deploy & Smoke Test
    runs-on: ubuntu-latest
    needs: build-and-push           # Only runs after image is published
    if: github.event_name == 'push'

    permissions:
      contents: read
      packages: read                # Required to pull from GHCR

    steps:
      # 1. Checkout — need scripts/smoke_test.py
      - name: Checkout code
        uses: actions/checkout@v4

      # 2. Set up Python for smoke test script
      - name: Set up Python 3.10
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      # 3. Install smoke test dependencies
      - name: Install smoke test dependencies
        run: pip install requests Pillow

      # 4. Log in to GHCR to pull the image
      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # 5. Pull the freshly published image
      - name: Pull latest image from GHCR
        run: |
          docker pull ghcr.io/${{ github.repository }}:latest
          echo "✅ Image pulled successfully"

      # 6. Start the container (detached, ephemeral deployment)
      - name: Start API container
        run: |
          docker run -d \
            --name cat_dog_smoke \
            -p 8000:8000 \
            ghcr.io/${{ github.repository }}:latest
          echo "Container started. Waiting for startup..."

      # 7. Wait for container to be healthy (poll /health for up to 60s)
      - name: Wait for API to be ready
        run: |
          echo "Polling http://localhost:8000/health ..."
          for i in $(seq 1 30); do
            STATUS=$(curl -s -o /dev/null -w "%{http_code}" http://localhost:8000/health || echo "000")
            echo "  Attempt $i: HTTP $STATUS"
            if [ "$STATUS" = "200" ]; then
              echo "✅ API is ready!"
              break
            fi
            if [ "$i" = "30" ]; then
              echo "❌ API did not become healthy after 60s"
              docker logs cat_dog_smoke
              exit 1
            fi
            sleep 2
          done

      # 8. Run smoke tests — FAILS pipeline if any check fails
      - name: Run smoke tests
        run: |
          python scripts/smoke_test.py --url http://localhost:8000 --wait 10
        env:
          PYTHONPATH: .

      # 9. Show container logs for traceability
      - name: Show container logs
        if: always()
        run: docker logs cat_dog_smoke

      # 10. Cleanup — stop and remove the ephemeral test container
      - name: Cleanup container
        if: always()
        run: |
          docker stop cat_dog_smoke || true
          docker rm cat_dog_smoke || true
          echo "Container cleaned up."

      # 11. Announce successful deployment
      - name: Deployment complete
        if: success()
        run: |
          echo ""
          echo "🎉 ============================================"
          echo "   DEPLOYMENT SUCCESSFUL"
          echo "   Image: ghcr.io/${{ github.repository }}:latest"
          echo "   All smoke tests passed ✅"
          echo "=============================================="

