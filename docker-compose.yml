# ============================================================
# docker-compose.yml — Cat vs Dog Classifier API
# ============================================================
# Usage:
#   docker compose up --build        # Build + start
#   docker compose up -d             # Start in background
#   docker compose down              # Stop
#
# Once running, open:
#   http://localhost:8000/health
#   http://localhost:8000/docs       (Swagger UI)
# ============================================================

services:
  cat-dog-api:
    build:
      context: .
      dockerfile: Dockerfile
    image: cat-dog-classifier:v1
    container_name: cat_dog_classifier
    ports:
      - "8000:8000"
    volumes:
      # Mount model volume as read-only for safety
      - ./models:/app/models:ro
    environment:
      - PYTHONUNBUFFERED=1
      - PYTHONDONTWRITEBYTECODE=1
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/health')"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

# ── Optional: Add a simple Swagger UI viewer ─────────────────────────────────
# Uncomment to browse the API with a separate Swagger UI container
# (the FastAPI /docs endpoint already provides this built-in)
#
# swagger-ui:
#   image: swaggerapi/swagger-ui
#   ports:
#     - "8080:8080"
#   environment:
#     - SWAGGER_JSON_URL=http://cat-dog-api:8000/openapi.json
#   depends_on:
#     - cat-dog-api
